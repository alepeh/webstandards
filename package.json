{
  "name": "webstandards",
  "version": "0.0.1",
  "description": "VanillaJS apps",
  "scripts": {
    "serve": "./buildRollup.sh && 'browser-sync dist -f dist --cors --no-notify --port 8000'",
    "cypress:open": "cypress open",
    "cypress:run": "cypress run --browser chrome --no-exit",
    "server": "node server.js"
  },
  "author": "Alexander Pehm",
  "dependencies": {
    "express": "^4.16.4",
    "lit-html": "^1.0.0-rc.2"
  },
  "devDependencies": {
    "browser-sync": "^2.26.3",
    "cypress": "^3.1.3",
    "rollup-plugin-browsersync": "^0.2.6",
    "rollup-plugin-node-resolve": "^4.0.0",
    "rollup-plugin-commonjs": "^9.2.0"
  }
}
