{
  "name": "webstandards",
  "version": "0.0.1",
  "description": "VanillaJS apps",
  "scripts": {
    "cypress:open": "cypress open",
    "cypress:run": "cypress run --browser chrome --no-exit",
    "serve": "./buildRollup.dv.sh && 'browser-sync dist -f dist --cors --no-notify --port 7000'",
    "server": "node server.js",
    "test": "jest --watch"
  },
  "author": "Alexander Pehm",
  "dependencies": {
    "express": "^4.17.3",
    "lit-html": "^1.0.0-rc.2"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.3.1",
    "browser-sync": "^2.26.3",
    "jest": "^24.1.0",
    "rollup-plugin-browsersync": "^0.2.6",
    "rollup-plugin-commonjs": "^9.2.0",
    "rollup-plugin-node-resolve": "^4.0.0"
  }
}
